#include "MaterialBlockDetails.h"

MaterialBlockDetails::MaterialBlockDetails(const dxm::Vector3& size, const dxm::Vector3& position, float standHeight)
	: m_Size(size), m_Position(position), m_StandHeight(standHeight)
{
}
